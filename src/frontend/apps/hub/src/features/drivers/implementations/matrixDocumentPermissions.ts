export const MATRIX_DOCUMENTS_EVENT_TYPE = "fr.gouv.hub.documents";

/**
 * A deliberately narrow Matrix power level: enough to write the documents
 * state event, below every moderation/state threshold in Hub-created rooms.
 */
export const DOCUMENT_CONTRIBUTOR_LEVEL = 1;
